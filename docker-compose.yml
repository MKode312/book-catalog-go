services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - book-catalog
    networks:
      - books_net
    restart: unless-stopped

  frontend:
    image: mkode312/book-catalog-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile.frontend
    expose:
      - "3000"
    environment:
      NEXT_PUBLIC_API_URL: /api
    depends_on:
      - book-catalog
    networks:
      - books_net
    restart: unless-stopped

  book-catalog:
    command: sh -c "/app/migrator && /app/book-catalog"
    depends_on:
      books-db:
        condition: service_healthy
      books-cache:
        condition: service_healthy
    image: mkode312/book-catalog-backend:latest
    build:
      context: .
      dockerfile: Dockerfile.backend
    env_file:
      - .env
    expose:
      - "8082"
    tty: true
    volumes:
      - ./config:/app/config:ro
      - ./migrations:/app/migrations:ro
    networks:
      - books_net
    restart: unless-stopped

  books-db:
    image: postgres:15
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - books_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - books_net
    restart: unless-stopped

  books-cache:
    image: redis:7-alpine
    networks:
      - books_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

networks:
  books_net:

volumes:
  books_postgres_data:
